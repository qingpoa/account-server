<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingpo.mapper.UserMapper">

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into user (
            username,
            password,
            nickname
        ) values (
            #{username},
            #{password},
            #{nickname}
        )
    </insert>

    <update id="updateUserInfo" parameterType="com.qingpo.pojo.user.UserVO">
        update user
        <set>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="avatar != null and avatar != ''">
                avatar = #{avatar},
            </if>
            <if test="enableOverspendAlert != null">
                enable_overspend_alert = #{enableOverspendAlert},
            </if>
        </set>
        where id = #{userId}
          and is_deleted = 0
    </update>

</mapper>
